<% if user_signed_in? %>
  	<%= link_to('Edit registration', edit_user_registration_path) %>
  <% else %>
  	<%= link_to('Sign Up', new_user_registration_path, class: "btn btn-default", style: "font:bold; color: #fff; background-color: #2ecc71") %>
<% end %>