
  <div class="contactContainer">
    <div class="contactContainer2">
      <div class="border-form-div">
        <h2 style="text-align: center">Sign up</h2>
          <hr class="colorgraph"><br>
            <div class="wrapper">
              <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
              <%= devise_error_messages! %>

              <p><%= f.label :first_name %><br />
              <%= f.text_field :first_name, autofocus: true, :placeholder => "First Name", class: "form-control" %></p>

              <p><%= f.label :last_name %><br />
              <%= f.text_field :last_name, autofocus: true, :placeholder => "Last Name", class: "form-control" %></p>

              <div class="field">
                <%= f.label :email %><br />
                <%= f.email_field :email, autofocus: true, :placeholder => "Email Address", class: "form-control" %>
              </div>

              <div class="field">
                <%= f.label :password %>
                <% if @minimum_password_length %>
                <em>(<%= @minimum_password_length %> characters minimum)</em>
                <% end %><br />
                <%= f.password_field :password, autocomplete: "off", :placeholder => "Password", class: "form-control" %>
              </div>

              <div class="field">
                <%= f.label :confirm_password %><br />
                <%= f.password_field :confirm_password, autocomplete: "off", :placeholder => "Confirm password", class: "form-control" %>
              </div>

              <div class="actions">
                <%= f.submit "Sign up",:class=>'btn btn-lg btn-primary btn-block', :style => "background-color: #2ecc71" %>
              </div>
            <% end %>

            <%= render "devise/shared/links" %>

          </div>
      </div>
    </div>
  </div>